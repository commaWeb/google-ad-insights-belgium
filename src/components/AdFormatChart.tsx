import { ResponsiveContainer, PieChart, Pie, Cell, Tooltip, Legend } from "recharts";

export const AdFormatChart = () => {
  return (
    <div className="h-[300px] flex items-center justify-center text-slate-500 text-center">
      <span>Ad format breakdown is not available in the public Google dataset.</span>
    </div>
  );
};
